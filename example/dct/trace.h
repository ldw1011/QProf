#include <set>
#include <vector>
template<class key, value>
class Statistics
{
  public:
    map<keys,do
};
class Instruction
{
  public:
    enum {MAX_UNIT_NUM=100000};
    Statistics(int id, int unit_id);
    bool operator<(const Statistics& st) const
    {
      double thiskey= this->id * MAX_UNIT_NUM +  this->unit_id;
      double rhskey=st.id * MAX_UNIT_NUM + st.utit_id;
      return (thiskey<rhkey);
    }
    static set<Statistics*> statistics; 
    int tot_access;
    int id;
    int unit_id;
    std::vector<
};




